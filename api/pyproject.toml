[project]
name = "vercel-fastapi-starter"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "fastapi>=0.121.0",
  "uvicorn>=0.34.0",
  "bcrypt",
  "pydantic[email]",
  "resend",
  "python-dotenv",
]

[tool.setuptools.packages.find]
include = ["auth*", "db*", "public*"]
